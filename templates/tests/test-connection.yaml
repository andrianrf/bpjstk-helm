apiVersion: v1
kind: Pod
metadata:
  name: "bpjstk-service-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: docker.io/curlimages/curl:latest
      command: ['curl', 'backoffice:8080/backoffice']
  restartPolicy: Never
